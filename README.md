# OpenClaw TraceBridge

Private bootstrap repo for the OpenClaw trace adapter + replay bus project.

## Goal
Build a low-overhead, Python-first trace adapter layer (Phase 1) and replayable trace bus (Phase 2) for OpenClaw.
